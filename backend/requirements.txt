# MediSense backend — pinned for the pilot vertical slice.
fastapi==0.115.5
uvicorn[standard]==0.32.1
SQLAlchemy==2.0.36
aiosqlite==0.20.0          # default dev datastore (zero-setup)
asyncpg==0.30.0           # PostgreSQL driver (production target)
pydantic==2.10.3
pydantic-settings==2.6.1
numpy==2.1.3              # in-memory cosine retrieval for the pilot
httpx==0.28.1            # optional Anthropic reasoning call
python-dotenv==1.0.1
